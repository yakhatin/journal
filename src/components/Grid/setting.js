import React from 'react';

const JournalSettings = (props) => {
    return (
        <React.Fragment>
            <div>
                
            </div>
        </React.Fragment>
    )
};

export default JournalSettings;